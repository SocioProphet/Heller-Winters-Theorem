{
  "$comment": "Semantic promotion constraints (C1/C2/D0 boundary) are enforced by Pydantic validators in claim_ledger_schema.py, not by this JSON Schema. This schema covers structural type constraints only.",
  "additionalProperties": false,
  "description": "Structural JSON Schema only. Semantic promotion constraints for the Heller-Winters prime/operator lane (C1/C2/D0 boundary and D0-locked operator rules) are enforced by Pydantic validators in schemas/claim_ledger_schema.py, not by this JSON Schema.",
  "properties": {
    "coordinate_convention": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Coordinate Convention"
    },
    "execution_mode": {
      "minLength": 1,
      "title": "Execution Mode",
      "type": "string"
    },
    "operator_ids": {
      "items": {
        "type": "string"
      },
      "title": "Operator Ids",
      "type": "array"
    },
    "runspec_id": {
      "minLength": 1,
      "title": "Runspec Id",
      "type": "string"
    },
    "window_schedule_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Window Schedule Id"
    }
  },
  "required": [
    "runspec_id",
    "execution_mode"
  ],
  "title": "RunSpec",
  "type": "object",
  "x-semantic-constraints": []
}
